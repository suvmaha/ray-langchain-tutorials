# Grafana + Prometheus

Cluster metrics dashboard for Ray workloads on EKS. Ray exposes Prometheus metrics natively — add Grafana to visualize throughput, latency, CPU/memory per worker, and task completion rates across the cluster.

> Coming soon
