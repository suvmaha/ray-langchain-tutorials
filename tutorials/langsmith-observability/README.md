# LangSmith Observability

Trace distributed LangGraph agent runs with LangSmith. See every tool call, model decision, and execution path across all Ray workers — not just aggregate metrics.

> Coming soon
